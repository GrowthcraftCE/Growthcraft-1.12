package growthcraft.core.shared.inventory.slot;

import net.minecraft.inventory.IInventory;

public class SlotInput extends GrowthcraftSlot {
    public SlotInput(IInventory inv, int index, int x, int y) {
        super(inv, index, x, y);
    }
}
