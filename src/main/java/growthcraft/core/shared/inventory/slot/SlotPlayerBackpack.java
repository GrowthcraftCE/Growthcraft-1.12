package growthcraft.core.shared.inventory.slot;

import net.minecraft.inventory.IInventory;

public class SlotPlayerBackpack extends SlotPlayer
{
	public SlotPlayerBackpack(IInventory inv, int index, int x, int y)
	{
		super(inv, index, x, y);
	}
}
